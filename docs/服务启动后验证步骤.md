# 服务启动后验证步骤

## ✅ 当前状态

所有Docker服务已成功启动：
- ✅ uss-postgres (数据库)
- ✅ uss-redis (缓存)
- ✅ uss-backend (后端API)
- ✅ uss-frontend (前端界面)

## 🔍 步骤1: 验证服务状态

```bash
# 查看所有服务状态
docker-compose ps

# 应该看到所有服务都是 "Up" 状态
# 如果看到 "Up (healthy)" 或 "Up" 都表示正常
```

## ⏳ 步骤2: 等待服务完全启动

```bash
# 等待服务完全启动（约30-60秒）
sleep 30

# 查看服务日志，确认没有错误
docker-compose logs backend
docker-compose logs frontend
```

## 💾 步骤3: 初始化数据库

```bash
# 初始化数据库并创建默认管理员账户
docker-compose exec backend python init_db.py

# 应该看到输出：
# ✓ 默认管理员账户创建成功
#   用户名: admin
#   密码: admin123
# ✓ 数据库初始化完成
```

## 🔍 步骤4: 测试API

```bash
# 测试后端API健康检查
curl http://localhost:5000/api/health

# 应该返回：
# {"status":"healthy","service":"Unified Security Scanner API"}
```

## 🌐 步骤5: 访问Web界面

### 方式1: 在Ubuntu虚拟机浏览器中

```
http://localhost:3000
```

### 方式2: 在Windows浏览器中（通过虚拟机IP）

```bash
# 在Ubuntu上获取IP地址
hostname -I
# 或
ip addr show | grep "inet "

# 然后在Windows浏览器访问：
# http://虚拟机IP:3000
# 例如：http://192.168.1.100:3000
```

### 登录信息

- **用户名**: `admin`
- **密码**: `admin123`

## ✅ 步骤6: 测试基本功能

1. **登录测试**
   - 使用 admin / admin123 登录
   - 确认能进入Dashboard

2. **项目管理**
   - 点击 "项目管理"
   - 创建测试项目
   - 确认项目创建成功

3. **扫描任务**
   - 点击 "扫描任务"
   - 创建测试扫描（选择任意类型）
   - 查看扫描结果

## 🎯 完成检查清单

- [ ] 所有服务都在运行（docker-compose ps显示Up）
- [ ] 数据库已初始化（执行init_db.py成功）
- [ ] API健康检查通过（curl返回healthy）
- [ ] Web界面可访问（http://localhost:3000）
- [ ] 能够成功登录（admin/admin123）
- [ ] 能够创建项目
- [ ] 能够创建扫描任务

## 📝 下一步

### 1. 配置GitHub（如果还没做）

```bash
# 在Ubuntu上
cd ~/projects/anquansaomiao

# 检查Git状态
git status

# 如果代码有修改，提交并推送
git add .
git commit -m "feat: 完成Docker部署配置"
git push origin main
```

### 2. 启用GitHub安全扫描

- 访问GitHub仓库
- Settings → Security → Code security and analysis
- 启用所有安全功能

### 3. 开始开发

- 阅读 [团队分工与Git工作流](docs/团队分工与Git工作流.md)
- 创建功能分支
- 开始开发功能模块

### 4. 学习使用

- 阅读 [用户使用手册](docs/用户使用手册.md)
- 测试各种扫描功能
- 查看扫描结果

## 🐛 如果遇到问题

### 问题1: 无法访问Web界面

```bash
# 检查服务状态
docker-compose ps

# 检查端口
sudo netstat -tulpn | grep 3000

# 检查防火墙
sudo ufw status
sudo ufw allow 3000
```

### 问题2: 登录失败

```bash
# 重新初始化数据库
docker-compose exec backend python init_db.py

# 查看后端日志
docker-compose logs backend
```

### 问题3: API无法访问

```bash
# 检查后端服务
docker-compose logs backend

# 检查数据库连接
docker-compose exec backend python -c "from app import db; db.create_all()"
```

## 📚 相关文档

- [用户使用手册](docs/用户使用手册.md)
- [API接口文档](docs/API接口文档.md)
- [团队分工与Git工作流](docs/团队分工与Git工作流.md)
- [GitHub安全扫描配置指南](docs/GitHub安全扫描配置指南.md)


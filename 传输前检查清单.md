# 传输前检查清单

## ✅ 在Windows上检查（传输前）

### 1. 确认项目文件完整

```powershell
# 在PowerShell中执行
cd E:\network\anquannchanpin

# 检查关键文件是否存在
Test-Path docker-compose.yml
Test-Path backend\app.py
Test-Path frontend\package.json
Test-Path .github\workflows\security-scan.yml

# 列出所有文件
Get-ChildItem -Recurse | Measure-Object | Select-Object Count
```

### 2. 检查文件大小

```powershell
# 检查总大小（不应包含node_modules等大文件）
Get-ChildItem -Recurse -File | 
  Where-Object { $_.FullName -notmatch 'node_modules|venv|__pycache__|\.git' } | 
  Measure-Object -Property Length -Sum | 
  Select-Object @{Name="Size(MB)";Expression={[math]::Round($_.Sum/1MB,2)}}
```

### 3. 确认.gitignore配置正确

确保以下目录/文件不会被传输：
- `node_modules/`
- `venv/`
- `__pycache__/`
- `.env`
- `*.log`

### 4. 如果使用Git，检查状态

```powershell
git status
git log --oneline -5  # 查看最近5次提交
```

## 📋 传输方法选择

根据您的环境选择：

- **VirtualBox/VMware**: → 使用共享文件夹（方法1）
- **已配置SSH**: → 使用SCP（方法2）
- **已配置GitHub**: → 使用Git（方法3，最推荐）
- **其他情况**: → 使用U盘或网络共享

## 🚀 快速开始

### 最简单的方法（GitHub）

**Windows上**:
```powershell
cd E:\network\anquannchanpin
git init
git add .
git commit -m "feat: 初始提交"
# 在GitHub上创建仓库后
git remote add origin https://github.com/your-username/unified-security-scanner.git
git push -u origin main
```

**Ubuntu上**:
```bash
cd ~/projects
git clone https://github.com/your-username/unified-security-scanner.git
cd unified-security-scanner
```

详细步骤请查看: `docs/Windows到Ubuntu代码传输指南.md`

